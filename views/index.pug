extends layout

block content
  h1= title
  p Welcome to #{title}  
  a(href="http://localhost:3000/new") ADD NEW MESSAGE
  ul
    each message in messages
      li #{message.user} - #{message.added}
        p= message.text

